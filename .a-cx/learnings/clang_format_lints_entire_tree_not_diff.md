---\nid: clang-format-entire-tree-ci\ntrigger: \"when running clang-format checks or pushing code to a PR\"\nconfidence: 0.95\ndomain: code-style\nscope: project\ndate: 2026-07-30\n---\n\n# Clang-format CI lints entire tree, not just diff\n\n## Action\n\nBefore committing or pushing, run clang-format across all three linted trees (`common/`, `rendererplugin/`, `audioelementplugin/`), not just your changed files—CI will reject pre-existing violations in files you didn't touch.\n\n## Evidence\n\n- Observed 3 times in session 1ddcd74d (2026-07-30):\n  - User discovered that clang-format CI failure on PR #17 pointed to a file in an earlier commit on the same branch\n  - The CI runs `find common -iname '*.h' -o -iname '*.cpp' | xargs clang-format --style=file:.clang-format --dry-run -Werror` (and the same for rendererplugin/, audioelementplugin/) — full tree, not diff\n  - AGENTS.md was updated to document the correct pre-commit check (three separate `find` calls matching the CI exactly)\n  - User verified the fix works by running the three commands locally and confirming \"OK\"\n\n## How to apply\n\nAfter every commit, before every push:\n```bash\nfind common -iname '*.h' -o -iname '*.cpp' | xargs clang-format --style=file:.clang-format --dry-run -Werror\nfind rendererplugin -iname '*.h' -o -iname '*.cpp' | xargs clang-format --style=file:.clang-format --dry-run -Werror\nfind audioelementplugin -iname '*.h' -o -iname '*.cpp' | xargs clang-format --style=file:.clang-format --dry-run -Werror\n```\n\nIf any fails, fix all violations in that tree (even ones you didn't introduce) and re-run all three checks before committing.\n"